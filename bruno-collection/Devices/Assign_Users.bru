meta {
  name: Assign Users
  type: http
  seq: 1
}

put {
  url: {{baseUrl}}/api/device-handler/devices/{{deviceId}}/assign-users
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {"users": ["{{userId}}"]}
}
